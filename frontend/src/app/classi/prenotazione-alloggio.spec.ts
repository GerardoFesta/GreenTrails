import { PrenotazioneAlloggio } from './prenotazione-alloggio';

describe('PrenotazioneAlloggio', () => {
  it('should create an instance', () => {
    expect(new PrenotazioneAlloggio()).toBeTruthy();
  });
});
